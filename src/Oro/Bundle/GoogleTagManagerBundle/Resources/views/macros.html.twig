{%- macro getPrice(prices) -%}
    {%- if prices is not empty -%}
        {%- set firstPrice = prices|first -%}
        {{- firstPrice.price|number_format(oro_locale_number_attribute('max_fraction_digits', 'currency'), '.', '') -}}
    {%- endif -%}
{%- endmacro -%}
